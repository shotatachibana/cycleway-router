# CLAUDE.md

このファイルは、Claude Codeがこのリポジトリで作業する際のガイドラインです。
**Codex等、他のコーディングエージェントを使う場合もこのファイルを読むこと**
(`AGENTS.md`を作る場合は、このファイルへの案内だけの薄いファイルにして、内容は
ここに一本化する)。

## プロジェクト概要

**自転車専用道路(車道から独立した、自転車のための道)だけをつなぎ合わせた
ネットワークで経路検索できるWebサービス**を作る。ユーザー本人が日常的に使い
やすいことを第一の目的とする個人プロジェクト。

1. 自転車専用道路の整備状況を**オープンデータ(主にOSM)**から取得・整形する
2. そのデータから「自転車専用道路だけの道路ネットワーク」を作る
3. 経路検索エンジンとして **R5**(Conveyal R5)を使う
4. 誰でもブラウザから使える形のプラットフォームを **GitHub Pages** で公開し、
   ユーザーが使いやすいWebサービスにする

## このファイルの位置づけ

`CLAUDE.md`は、Claude Codeが起動するたびに自動で読み込む**プロジェクト全体の
文脈**(ずっと有効な前提知識・恒常的なルール)。直近の作業状況を書く場所では
ないので、日々書き換える想定にしない。

これとは別に、個別のタスクごとに「今回何を作ってほしいか」をまとめた
**指示書(`src/<タスク名>/instruction.md`)**を都度作る運用にする。

**生成AIがこれまでの作業経緯を思い出すための情報源は`CLAUDE.md`(恒常ルール)と
`log.md`(新しいものが一番上の詳細な作業記録)の2つ**。セッション開始時に
「前回の続きから」と言われた場合や、何に取り組んでいたか不明な場合は、
`log.md`の上から数エントリを確認してから作業に入ること。

## フォルダ構成

```
(リポジトリルート)
├── CLAUDE.md              # このファイル(プロジェクト全体の文脈)
├── README.md              # 使い方(セットアップ・ビルド・公開手順)
├── log.md                 # 生成AIとのやり取りの作業記録(日付・時刻つき、新しいものが上)
├── docker-compose.yml     # R5実行環境などのコンテナ定義
├── data/
│   ├── raw/                 # 生データ(OSM PBF、自治体オープンデータ等。gitignore対象)
│   ├── interim/             # 前処理の途中データ(gitignore対象)
│   ├── processed/           # 自転車専用ネットワーク等の成果データ
│   ├── manual/              # ユーザーが手動で追加・修正する専用道路区間
│   │                          (data/rawから再計算できない一次情報のため、
│   │                          processedと違いGit管理対象。2026-09-22追加。
│   │                          入力方式・スキーマは検討中→
│   │                          src/network_merge/instruction.md参照)
│   └── datasets.txt         # 使用データの一覧(出典URL・取得日・ライセンス・更新頻度)
├── src/                   # データ取得・整形・ネットワーク構築・R5実行スクリプト
│   └── <タスク名>/
│       ├── instruction.md   # そのタスク用の指示書(下記参照)
│       └── (コード本体)
├── results/               # 検証結果の表・図(出力先はすべてここに集約。
│                            新しいトップレベルフォルダを勝手に作らないこと)
├── docs/                  # GitHub Pagesで公開する静的サイト(フロントエンド)
│   ├── index.html
│   ├── assets/
│   └── data/                # フロントが読む軽量な配信用データ(GeoJSON/PMTiles等)
└── test/                  # 簡単な検証スクリプト
```

※ GitHub Pagesの公開元は`docs/`を想定(Settings → Pages → Branch: main / `/docs`)。
　別方式(`gh-pages`ブランチ、GitHub Actionsでのデプロイ)を採る場合は、その理由を
　`log.md`に残してからこの節を更新すること。

## ゴールと非ゴール

**ゴール**
- **自転車専用道路のみ**を通る経路を検索できること(基本方針は「専用道路以外は
  走らない」。ただし交差点の横断など例外の扱いは、下記「専用道路の定義」と
  「設計上の未決事項」に従う)
- データの出典・取得日・ライセンスを追跡でき、データ更新時に再ビルドできること
- ブラウザ上で出発地・目的地を指定して経路が地図に表示される、ユーザー本人が
  使いやすいUIにすること
- 有料API・課金設定なしで一通り動く構成を基本とすること
- 見つけた経路をGarmin等のサイコン(GPSサイクルコンピュータ)に取り込めること
  (2026-09-21追加。GPX形式でのダウンロード機能を`docs/assets/main.js`に実装済み。
  サーバー不要でブラウザ内のみで生成)
- Googleマップの経路検索のように、地図上部の常時表示パネル(`#directions-panel`)
  で目的地→出発地の順に指定するだけでルートを検索できること(2026-09-22追加。
  詳細は下記「常時表示の出発地・目的地パネル」参照。現在地
  (`navigator.geolocation`、許可されれば)を出発地として自動的に使う)

**非ゴール**
- 公共交通・徒歩を組み合わせた経路検索(R5は対応しているが、このプロジェクトの
  スコープ外。必要になればユーザーと相談して拡張する)
  - **将来の検討事項(輪行対応、2026-09-21)**: ユーザーから「R5を活かして
    自転車+鉄道(輪行)のマルチモーダル経路検索に対応させたい。サイクルトレイン
    (輪行袋不要で自転車をそのまま持ち込める路線)を優先、混雑路線は回避、
    という設定もしたい」という要望があった。実現可能性を調査した結果:
    - GTFSデータ: 公共交通オープンデータセンター(ODPT, odpt.org)が無料だが
      要登録・APIキー取得。ただし関東の主要私鉄(JR東日本・西武等)の多くは
      標準GTFS形式ではなく独自API形式でしか提供されておらず、関東全体を
      揃えるには事業者ごとの変換作業が必要
    - R5とGTFSの互換性: R5はGTFSベースのマルチモーダル経路探索が元々の
      主目的だが、日本のGTFS-JP(独自拡張フィールド入り)で問題なく動くかは
      未検証
    - サイクルトレインの実例(2026-09-21時点で確認できたもの): JR常磐線
      (上野〜土浦、輪行袋不要。2026年4月から浜通り区間でも開始)、西武池袋線・
      秩父線(S-TRAIN、土休日1往復・要予約。ただし運行中止時の備えとして
      輪行袋携行が推奨されている)、関東鉄道常総線。ユーザーの記憶にあった
      「北鉄」に該当する路線は未確認。この種の情報はGTFSに存在しないため、
      いずれにせよ手動でのキュレーション(路線ごとの独自設定ファイル)が前提
    - 混雑データ: 国交省が「都市鉄道混雑率調査」を毎年無料公開しているが
      PDF形式で、機械可読なCSV/APIとしての整備は限定的
    - **結論**: 試用版として着手するなら、JR東日本・都営地下鉄などGTFS-JPが
      確実に手に入る一部事業者・一部路線に絞った小規模なプロトタイプから
      始めるのが現実的。現時点では未着手(ユーザーの意向により、まずは
      ドキュメント化のみに留める)
- 全国網羅(まず対象地域を絞る。**対象地域: 関東地方(2026-09-20、ユーザー方針)**。
  ただしこれは初期のOSMデータ調査(延長・連結性の集計)の範囲であり、集計結果を
  見たうえで市区町村単位等への絞り込みを検討する)
- リアルタイム交通・信号待ち時間・路面状態などの詳細なコスト評価(後日の拡張)

## 「専用道路」の定義(2026-09-20、ユーザー方針)

ユーザーの方針は「**基本的には、自転車専用道路しか通りたくない**」。したがって
対象は、車道から独立した自転車専用の道に絞る。

- **対象(専用に含める)**:
  - OSMの `highway=cycleway`(独立したウェイ)。自転車専用のものも、歩行者
    共用のサイクリングロード(`foot=yes`、`segregated=*`等)も含める
  - 車道沿いの分離型自転車道(縁石・柵等で車道から分離されたもの。OSMでは
    `cycleway=track`・`cycleway:left/right=track`として道路ウェイ側に
    タグ付けされていることが多い)も含める。この場合、道路ウェイそのものを
    通すのではなく、自転車道部分だけをネットワーク上で表現する必要がある
    (OSMのタグ表現の違いを吸収する処理が必要。方法は`instruction.md`で
    設計し、ユーザーに確認すること)
- **対象外(「専用道路」本体には含めない)**: 自転車専用通行帯
  (`cycleway=lane`、車道上のペイント区分)、矢羽根・ナビライン・ナビマーク
  (`shared_lane`等)、車道混在、一般の歩道(自転車通行可の指定がないもの)
  - **2026-09-20追記1**: ユーザーから「専用度合いをもっと細かく見せてほしい」との
    要望があり、東京都都市整備局の自転車関連情報マップ(wagmap、区部・多摩部・
    島しょ部対象)を参考に、フロントエンドでは`cycleway=lane`・
    `cycleway=shared_lane`系も「参考レイヤー」(デフォルト非表示、チェックボックス
    で表示切替)として地図に重ねて表示するようにした
    (`src/export_web/export_cycleway_network.py`参照)
  - **2026-09-20追記2**: 「近距離検索が実用的でない」というユーザーの指摘を受け、
    上記の参考レイヤー(lane・shared_lane)を**近距離検索(ブラウザ内Dijkstra)にも
    ペナルティ付きで使う**ように変更した(専用道路=重み1.0、lane=重み2.5、
    shared_lane=重み5.0。`docs/assets/graph.js`)。長距離ルートの事前計算
    (R5のカスタムコスト)は引き続き専用道路本体のみを低コストとする設計で
    変更していない。連結性を再集計したところ、全カテゴリ合計で総延長は
    2,847km→約4,008kmに増えたが最大連結成分は69km(全体1.7%)のままで、
    関東規模での分断傾向は変わっていない(局所的な近距離検索のヒット率は
    向上する見込み)
  - **2026-09-21追記**: それでも近距離検索が実用的でないというユーザーの
    指摘を受け、**一般道路網(車も通る普通の道、重み8.0)もタイル方式で
    近距離検索に含める**ようにした(`src/routing_tiles/`)。関東の一般道路網
    (`highway=cycleway`/`track`を除く。専用道路網側で既にカバーしているため)を
    0.1度四方(約11km四方)のタイルに分割し`docs/data/tiles/`に411個の
    GeoJSON(最大8MB、中央値0.6MB)として静的配置。出発地・目的地を
    クリックした時点で該当タイルだけをその都度取得し、ルーティンググラフに
    動的統合する。初期表示は軽いまま(専用道路網4.7MB+参考レイヤー1.9MB)、
    クリックした場所周辺は一般道込みで探索できるため、同一タイル内
    (だいたい徒歩・自転車で行き来する距離感)の近距離検索は実用的に
    繋がるようになった。タイルをまたぐ長距離は引き続き「経路なし」になるため、
    そちらは事前計算ルート(R5)を使う運用に変わりない
- **交差点・横断歩道など道路を渡る短い区間(2026-09-20 ユーザー方針)**:
  **基本的には許容しない**。ただし「専用道路だけでは経路が見つからないなど、
  とても難しい場合」に限って許容する。実装上は次の2段階とする:
  1. まず横断区間なし(厳密モード)で検索する
  2. 経路が見つからない場合のみ、横断区間を許容したモードで再検索する。
     このとき横断回数・横断区間の長さを最小化する(ペナルティを大きくする)
     こと、結果の地図・表示上で横断区間を通常区間と区別して明示すること
  - 「とても難しい場合」の具体的な基準(横断の最大回数・1回あたりの最大長・
     どの種類の道路〈信号あり/なし、車線数〉の横断まで許容するか)は、連結性の
     集計結果を見てからユーザーと合意する
  - **実装上の注意**: R5は道路ごとに任意のコスト(横断ペナルティ)を設定する
    仕組みを標準では持たないと思われる(要確認)。厳密モード用と横断許容
    モード用でネットワークを2種類ビルドして順に検索する、または自前の
    グラフ探索でコストを扱う、などの方式を比較して提案すること
- **一般道での専用道路クラスタ間の接続(2026-09-20、ユーザー方針。追加)**:
  `src/network_connectivity/`での連結性分析の結果、関東地方全体では専用道路
  ネットワークが数百m〜数kmスケールで強く分断されており(最大連結成分でも
  全体の2.5%程度)、「短い横断の許容」だけでは実用的な経路(例:
  品川区⇔高崎市、直線距離約102km)がほとんど見つからないことが判明した。
  これを受けて、ユーザーは「設計上の未決事項」2の(iii)案(専用道路どうしを
  結ぶ一般道を、長さの上限つき・ペナルティつきで許容し、専用道路以外の区間を
  地図上で明示する)を採用する方針を示した(2026-09-20)。
  - 上記の「交差点の短い横断」(数m〜数十m、信号交差点の通過などを主に想定)とは
    別に、**専用道路クラスタどうしを繋ぐための、数百m〜数kmの一般道区間**を
    許容するモードを追加する。両者は目的も長さのオーダーも異なるため、
    実装・パラメータ(上限長・ペナルティ)を分けて設計すること
  - 一般道区間の長さ上限・ペナルティの重み・合計許容距離(経路全体に対する
    割合か絶対値か)は未確定。**次のタスクで具体的な数値をユーザーと合意する**
  - この方式を実装するには、専用道路ネットワークだけでなく**一般道路網
    (OSMの通常の道路データ)も対象地域について取得する必要がある**。データ量・
    取得方法(Geofabrik PBF切り出し等)は着手前にユーザーに報告し確認する
  - LTS(走行ストレス)による重み付けとの併用は今回は採用しない(ユーザーは
    「専用道路を優先し、繋ぐ部分だけ一般道を最小限に使う」方式((iii))を選択。
    2026-09-20)。ただし将来的に一般道区間の質を区別する必要が出た場合の
    拡張候補として残す
- OSMの日本でのタグ付けは統一見解が弱いため、この定義は`config`に外出しし、
  条件を変えて連結性・延長を比較できるようにする
- 定義の変更・確定は`log.md`に残す

### データソースの優先順位(2026-09-22、ユーザー方針)

OSM単独では「変なところで途切れる・不正確な区間が混ざる」という問題がある
ため、`docs/data/cycleway_network.geojson`(専用道路ネットワーク本体)の
データソースに優先順位を設ける。

1. **手動ネットワーク**(最優先。入力方式・スキーマは未確定→保留。
   `data/manual/manual_network.geojson`にユーザーが直接配置する想定で
   ひとまず実装している)
2. **自治体が提供するネットワーク**(2番目。2026-09-22、東京都建設局
   「自転車走行空間について」(CC-BY-4.0)を取得・組み込み済み。ただし実データを
   確認したところ「自転車推奨ルート」218区間中174区間、「優先整備区間」
   112区間中76区間が**未整備・計画中**で、かつ整備済区間も車道分離型か
   ペイントレーンかを区別する属性が無かったため、ユーザーと協議のうえ
   **整備済系のみ(計80区間)をtier5(分離型自転車道、近似)相当として**採用した
   (詳細は`src/municipal_data/instruction.md`、経緯は`log.md` 2026-09-22
   02:42参照)。他の関東自治体は自転車ネットワーク計画をPDF/画像でのみ公開して
   おり機械可読データが無い。国交省が2026年1月に「自転車ネットワークデータ
   (BNデータ)」標準仕様を策定しているため、今後の自治体側の対応を継続的に
   確認する価値がある。新しい自治体データを追加する際も、実データを確認して
   から取り込み範囲・tierをユーザーと確認すること(`data/raw/`が空の場合の
   確認ルールに従う)
3. **OpenStreetMap**(最後の補完。従来通り`src/osm_survey`+
   `src/export_web/export_cycleway_network.py`)

同じ場所に複数ソースが重複する場合は、**優先度の低い方(主にOSM側)を
空間的に間引く**(線全体を消すのではなく、重複する部分だけをトリムする)。
実装は`src/network_merge/`(`build_priority_network.py`)。手前の
`export_cycleway_network.py`はOSM由来の中間ファイル
(`data/interim/cycleway_network_osm.geojson`)を出すだけに役割を変更し、
最終成果物の組み立ては`network_merge`が行う。詳細は
`src/network_merge/instruction.md`参照。

### 「サイクリングロード」の強調表示(高速道路風)は個別登録制(2026-09-22、ユーザー方針)

上記のtier1〜5ネットワーク(`docs/data/cycleway_network.geojson`、自動取得・
自動分類)とは別に、多摩川サイクリングロードのような著名な専用道路を
「高速道路風」(濃い緑の縁取り+太字の名前バッジ)で強調表示する機能がある。

**この強調表示の対象は、OSMのtier・name・区間の長さ等からの推測に一切頼らず、
ユーザーが道路名を個別に指定したものだけに限定する**(過去に複数回、交差点の
断片や無関係な道路がヒューリスティックで誤って強調されてしまう問題が起きた
反省による)。具体的な運用:

- 強調表示の対象一覧は`docs/data/cycleway_network.geojson`とは完全に別の
  ファイル`docs/data/highway_routes.geojson`に登録する
  (`{"properties":{"name":...},"geometry":{LineString/MultiLineString}}`の
  シンプルなスキーマ)。フロント(`docs/assets/main.js`)の
  `highway-route-casing`/`highway-route-badge`レイヤーはこのファイルだけを見る
- 対象の**選定(どの道路名を対象にするか)は必ず人間が行う**。ジオメトリの
  作成作業(座標の抽出)はOSMを活用してよい(選ぶのは人間、作業を楽にするのは
  機械、という役割分担)。具体的には`src/highway_routes/config.yaml`の
  `routes`にユーザーが道路名(必要ならOSM上の表記ゆれ・別名も`aliases`に)を
  追記し、`extract_named_routes.py`がOSMの`name`タグ**完全一致**(部分一致は
  しない)でジオメトリを抽出して`docs/data/highway_routes.geojson`を再生成する
- 一方、それ以外(自転車道が整備された一般の車道等、tier1〜5全般)は従来通り
  OSM・自治体データから自動取得・自動分類してよく、高速道路風にはしない
  (通常の色分け表示のみ)。経路探索の対象は引き続きこちら側

**配色(2026-09-22追記)**: 「普通の線も緑だと高速道路風の登録済みルートと
紛らわしい」という指摘を受け、**緑色は登録済み「サイクリングロード」
(`highway-route-casing`/`highway-route-line`/`highway-route-badge`)専用の色
として予約**し、tier1〜4(`cycleway-line-solid`、OSM・自治体データ由来の
自動分類)は青系(`TIER_STYLE`参照)に変更した。日本の道路標識の配色
(高速道路=緑、一般国道=青)をヒントにしている。登録済みルート上では、
青い通常線を`highway-route-casing`+`highway-route-line`(緑)が完全に覆い隠す
よう重なり順を制御している(`docs/assets/main.js`、`loadCyclewayNetwork()`の
完了を待ってから`loadHighwayRoutes()`を呼ぶ)。

## パイプライン構成

```
[1] fetch_lane_data     … OSM(主)と自治体データ(検証・補完)から自転車専用道路を取得
  → [2] build_network   … 「専用道路の定義」に沿ってOSMを絞り込み、自転車専用道路
                          だけのネットワーク(OSM PBF等)を作る
  → [3] run_r5          … R5で経路検索(ローカルまたはサーバ上)
  → [4] export_web      … Webから使える形式に変換して docs/ へ出力
  → [5] frontend        … GitHub Pages上のUI(地図・出発地/目的地入力・経路表示)
```

各ステップは独立したスクリプトとして実装し、中間ファイルをディスクに残す
(どこかのステップだけやり直せるようにする)。詳細な入出力形式は
`docs/pipeline_spec.md`ではなく、公開サイトと混ざらないよう
`src/<タスク名>/instruction.md`に書く。

## 設計上の未決事項(重要。着手前に必ずユーザーと確認すること)

以下は論理的な飛躍・不確実性があるため、**勝手に決めずに提案してユーザーの
判断を仰ぐ**こと。

1. **採用方針(2026-09-20、ユーザー確認済み): (C) クライアントサイド方式**。
   R5はネットワーク構築時の検証(連結性確認・可視化等)に使い、実際にブラウザ上で
   動く経路検索は、配信したグラフ(GeoJSON等)に対するJS側の最短経路探索
   (Dijkstra/A*等)で行う。「経路検索エンジンとしてR5を使う」という当初方針からは
   外れる点はユーザーに説明済み。GitHub Pagesは静的ホスティングでありR5
   (Javaサーバ)をそこで動かすことはできないため、この対応が必要だった。
   - **2026-09-20 追記1**: 当初「自転車専用ネットワークは小さくなる想定」で(C)を
     選んだが、その後「専用道路クラスタ間を一般道で接続する((iii)方式)」の採用
     ([専用道路の定義]参照)に伴い、一般道路網も含めて全てクライアントサイドに
     配信し、任意地点間検索を維持したい、という方針をユーザーが一時検討した。
     `src/road_network/`で実測したところ、関東地方の一般道(motorway/footway等を
     除く)は**183万way・950万ノード**、GeoJSONで**449MB(gzip圧縮後65MB)**に
     達し、ダウンロード量だけでなくブラウザ側でのグラフ保持・探索処理自体が
     非現実的な規模と判明した(詳細は`results/tables/road_network_size_comparison.csv`
     ※作成予定)。
   - **2026-09-20 追記2**: 上記の実測を踏まえ、一時(B')ユーザー自身のPCを
     常時稼働サーバーとして使う方式を検討し、実際にConveyal R5をフォークして
     専用道路優先のカスタムコスト(`src/r5_custom_cost/`)を実装、関東本土
     全域のネットワーク構築・品川⇔高崎(158.7km、74.7%が専用cycleway上)の
     経路検索に成功するところまで実装・検証した。
   - **2026-09-20 追記3(最終決定)**: ユーザーが「サーバーを常時運用する方針は
     やめたい」と判断したため、**常時稼働サーバーは持たない**方針に変更した。
     `src/r5_custom_cost/`で実装したR5フォークは無駄にはならず、**役割を
     「常時稼働API」から「オフラインでのルート事前計算ツール」に変更**して
     引き続き使う。最終的なアーキテクチャは (A)+(C) のハイブリッド:
     - **近距離(専用道路ネットワーク単体で完結する範囲)**: (C)クライアント
       サイド。専用道路ネットワーク(約8MB)のみをブラウザに配信し、JS側で
       即座に探索する。任意地点間・オフラインでも動く
     - **長距離・専用道路クラスタをまたぐ経路(例: 品川⇔高崎)**: (A)事前計算
       方式。開発コンテナ内でカスタムコスト入りR5を都度手動実行し、計算結果の
       ルート(GeoJSON)を静的ファイルとして`docs/data/`に配置する。任意地点間
       検索はできず、あらかじめ計算しておいた行き先の一覧から選ぶ形になる。
       新しい行き先が欲しくなったら、その都度開発コンテナ内でR5を再実行して
       追加する運用
     - サーバー・トンネル・ポート開放は一切不要。完全に静的なGitHub Pagesで完結し、
       クラウド費用も発生しない
   検討した候補(参考として残す):
   - (A) **事前計算方式**: R5(またはr5py/r5r)で主要な出発地・目的地の組を
     事前計算し、結果を静的ファイルとして`docs/data/`に置く。任意の地点間検索は
     できないが、完全に静的で無料
   - (B) **別ホスティングのAPI方式**: R5をクラウド(Cloud Run等)で動かし、
     GitHub PagesのフロントからAPIを呼ぶ。任意地点間検索ができるが、運用・
     コスト・メモリ(R5は比較的メモリを使う)の検討が必要
   - (C) **クライアントサイド方式**: 自転車専用ネットワークは小さくなる想定なので、
     グラフをGeoJSON等で配信し、ブラウザ側のJSで最短経路探索する
     (R5は「ネットワーク構築・検証用」に使う)。静的で任意地点間検索ができる
     が、「経路検索はR5で」という当初方針からは外れる
   - (D) **ローカル利用方式**: R5はローカルのDockerで動かし、GitHub Pagesは
     説明・データ公開のみ
2. **専用道路だけだと、ネットワークが強く分断され、経路が見つからないことが
   多い**と予想される(専用道路は交差点・横断部分で切れやすく、そもそも
   離れ小島状に存在する)。まず実データで連結成分の数・延長・最大成分の割合を
   出し、その結果を見てから、次のどれにするか提案すること:
   (i) 完全に専用道路のみ(見つからなければ「経路なし」と表示) /
   (ii) 交差点等の短い横断のみ許容(**ユーザー方針は「厳密モードで見つから
   ない場合のみ横断を許容」の2段階。「専用道路の定義」参照**) /
   (iii) 専用道路どうしを結ぶ一般道を、長さの上限つき・ペナルティつきで許容し、
   専用道路以外の区間を地図上で明示する
   **→ 2026-09-20、`src/network_connectivity/`の連結性分析の結果
   (関東地方全体では最大連結成分が全体の2.5%程度しかなく、例えば
   品川区⇔高崎市のような約100km規模の経路は(ii)だけでは見つからないことが
   判明)を受けて、ユーザーは(iii)の採用を決定。詳細は「専用道路の定義」の
   「一般道での専用道路クラスタ間の接続」を参照**。
   また、出発地・目的地が専用道路上にあるとは限らないため、
   最寄りの専用道路へのアクセス区間(徒歩・押し歩き等)の扱いも決めること。
   R5にはOSMのタグから自転車の走行ストレス(LTS: Level of Traffic Stress)を
   推定して制限する機能があるため、「ネットワークを物理的に絞る」方式と
   「LTS等の制限で絞る」方式の比較も行うこと(2026-09-20時点ではLTS方式は
   不採用。「専用道路の定義」参照)
3. **データ調査の結果(2026-09-20時点の事前調査)**。
   - 自治体・国のオープンデータで、専用道路をGISデータで公開しているものは
     東京都建設局(整備済区間2019年度末・都道中心のシェープファイル、CC BY 4.0
     と推定・要確認)を確認できた程度。多くの自治体は整備状況をPDFで公開する
     にとどまる。国交省・国土数値情報に該当データは見つからなかった。
     商用ではMapFan DBの自転車ルーティング用データがある(有償)
   - したがって**主データはOpenStreetMap**(`highway=cycleway`)とし、自治体
     データは検証・補完用とする。OSMはODbLのため、加工したネットワークを
     公開する際の出典表記・ライセンス上の扱いを公開前に確認すること
   - **未実施**: 対象地域でのOSMの入力状況の定量確認(Overpassで
     `highway=cycleway`の延長・`foot`/`segregated`等の属性・`cycleway=track`の
     分布を集計する)。対象地域が決まり次第、最初のタスクとして実施する
   - 「専用道路の定義」のうち、横断を許容する基準などの数値は、この集計結果を
     見せたうえでユーザーと合意する
4. **R5のバージョン・実行環境**(必要なJDKのバージョン、r5r/r5pyなどラッパー
   を使うかJARを直接使うか)は、着手時に公式ドキュメントで最新情報を確認する
   (記憶に頼らない)

## 技術スタック(暫定。確定次第この節を更新)

- **経路検索**: R5(Conveyal)。呼び出し方法(JAR直接 / r5py / r5r)は要検討
- **道路データ**: OpenStreetMap(PBF)。Geofabrik等から取得
- **ネットワーク加工**: Osmium / osmosis / Python(GeoPandas, pyrosm等)。
  【要記入: 選定後に確定】
- **自転車専用道路データ**: OSM(主)。自治体オープンデータは検証・補完用
- **フロントエンド**: 静的サイト(HTML/JS、ビルド不要)。地図は**MapLibre GL JS
  + OpenFreeMap**(ベクトルタイル、2026-09-20確定)。
  - 当初Leaflet+Esriの無料タイル(services.arcgisonline.com)を使っていたが、
    調査の結果Esriの無料タイルはレガシー扱いで、新サービスはArcGIS開発者
    アカウント+APIキーが必要と判明し不採用(「無料・キー不要」方針に反するため)。
    CartoDB Positronも同様の理由(実機確認でAPIキー要求の透かしが表示された)で
    不採用
  - OpenFreeMapは登録・APIキー・レート制限が無く恒久的に無料(寄付運営)。
    ベクトルタイルなので道路・鉄道の太さ・色を自分でカスタマイズでき、
    Googleマップに近い見やすさを実現しやすい
  - タイル・ジオコーダー等は**無料・キー不要で規約上問題ないもの**を選ぶ方針は
    変わらず。今後別のタイル提供元を検討する際も、利用規約の実機確認
    (レガシー/deprecated扱いでないか、実際にタイルを描画してAPIキー要求の
    透かし等が出ないか)を必ず行うこと(2026-09-20の教訓)
  - **デザイン方針(2026-09-21、ユーザー方針)**: 自転車専用サイトなので、
    自動車の高速道路・幹線道路や鉄道より自転車インフラ(専用道路・レーン等)の
    方を視覚的に目立たせる。標準のliberty styleはmotorway/trunk/primary等を
    濃いオレンジ・太い線で強調するが、これを減彩・細線化している
    (`docs/assets/main.js`の`mutedCarRoadLayer`)。建物の立体表示(fill-extrusion)
    も見づらいため平面化。鉄道は当初薄すぎたため濃く太くしたが、その後
    自転車専用道路の方をズーム連動でさらに太く・縁取り付きで強調し直し、
    鉄道より確実に目立つようにした(`zoomScaledWidth`、登録済み
    サイクリングロードの縁取りは`highway-route-casing`)。地名ラベルは
    英語併記をやめ日本語のみ表示にしている(`japaneseOnlyTextField`)。
    **2026-09-22追記**: 駅も目立たせたいという要望を受け、一度
    `poi_transit`レイヤーの`icon-size`を0.9→1.3にしたが、このレイヤーは
    `class in [airport, bus, rail]`をまとめて描画しており、結果的に
    数の多い**バス停**ばかりが目立つ形になってしまった(ユーザー指摘)。
    さらに調査(`map.queryRenderedFeatures`で実データを確認)したところ、
    **実際の駅データのclassは"railway"であり、そもそも`poi_transit`の
    filter(`class in [airport,bus,rail]`)に一致せず一度も描画されて
    いなかった**と判明した。対応として`poi_transit`はバス・空港のみの
    元の状態に戻し、`class === "railway"`を対象にした専用レイヤー
    `station-dot`(丸、色は鉄道路線と同じ`RAIL_JR_COLOR`)+`station-label`
    (太字ラベル)を新設した(スプライト画像を使わないcircleレイヤーなので
    画像欠落の心配が無い)
  - **モバイル対応・レイアウト(2026-09-21〜22)**: サイドバーを常時表示の
    固定カラムではなく「☰」ボタンで開閉するオーバーレイパネルにし、地図を
    画面全体に表示する(Googleマップに準じたUI)。デスクトップ・モバイル
    共通の仕組みで、画面幅768px超では初期状態で開いておく。「☰」(丸ボタン)を
    クリックするとボタン自体が消え、サイドバー右上にプレーンな「×」
    (`#sidebar-close`、丸枠は無し)が現れる。同時に背景の地図に半透明の
    黒いオーバーレイ(`#sidebar-backdrop`)が重なって薄暗くなり、これを
    クリックしても閉じる。ズームボタンは左上のmenu-toggleと重ならないよう
    右下に配置。専用度合いの凡例(`#map-legend`)はメニューではなく地図上に
    常時表示(位置は右上。初め左下にしたが「右上の方が良い」との指摘で変更)。
    2026-09-22時点でサイドバーの中身は「表示オプション」「長距離ルート
    (事前計算済み)」「出典」のみで、近距離検索(出発地・目的地の指定や結果
    表示)はサイドバーではなく下記の`#directions-panel`が担う
  - **常時表示の出発地・目的地パネル(2026-09-22、ユーザー方針:
    「Googleマップをパクって」)**: `#directions-panel`が地図左上に常時表示され、
    メニューを開かなくても経路検索できる。**Googleマップの経路パネルを
    模した段階的な流れ**にしている:
    1. 初期状態では目的地の行だけが見える(プレースホルダ「目的地を入力するか、
       地図をクリック」)。地名検索(Nominatim)または地図クリックで指定できる
    2. 目的地が決まると、出発地の行(「出発地を入力するか、地図をクリック」)が
       自動的に現れる(`revealOriginRow`)。ページ読み込み時に
       `navigator.geolocation`で現在地取得を試みており、取得できていれば
       この時点で入力欄に「現在地」と表示され、追加操作無しにそのまま
       ルート検索される(拒否・失敗時は黙ってフォールバックし、地名検索・
       地図クリックで手動指定できる)
    3. 両方揃うと自動的に近距離検索を実行し、結果(距離・セグメント内訳・
       GPXダウンロードボタン)をこのパネルの中に直接表示する(以前は
       サイドバーを自動で開いて表示していたが、Googleマップの経路パネルに
       似せて変更した)
    - 地図クリックの対象(出発地か目的地か)は明示的な「①/②」ボタンではなく
      `nextPickKind()`(目的地が無ければ目的地、あれば出発地、両方あれば
      クリックしても何もしない)で自動判定する。道路情報ポップアップ
      (`attachRoadInfoPopups`)も同じ関数で「指定待ち中は出さない」を判定する
    - 入れ替えボタン(⇅、`#btn-swap-points`)とクリアボタン(×、
      `#directions-clear`、パネル右上)を備える。入れ替えボタンは出発地・
      目的地の2行をまとめる`#directions-inputs`(position:relative)の右端に
      絶対配置しており、2行の間に空欄ができないようにしている
      (2026-09-22、以前は2行の間の独立したブロックだったため空欄に見えると
      指摘された)
    - 現在地マーカーはGoogleマップの青い点を模した`.current-location-dot`
      (通常の出発地ピンとは別のCSSクラス)。出発地(`.pin-from`)は白縁取りの
      緑丸、目的地(`.pin-to`)はCSSのみの水滴型ピン(`border-radius:
      50% 50% 50% 0`+`rotate(-45deg)`、anchor:"bottom"で先端が実座標を指す)。
      以前は色付き丸に「出」「着」と日本語を表示していたが、
      「そういう表示はやめてほしい」との指摘で変更した(2026-09-22)
    - デスクトップでサイドバーが開いている時はパネルをその右隣にずらし、
      モバイル幅ではサイドバーが画面の大半を覆うため一時的に隠す
      (`updateDirectionsPanelPosition`、閉じれば自動的に戻る)
    - 近距離検索の結果ルートはGoogleマップと同じ青(`ROUTE_RESULT_COLOR
      = "#4285f4"`)。区間(一般道路/専用道路/自転車レーン/車道混在)の
      切り替わり地点には高速道路のJCTマーカーと同じ白丸
      (`search-result-junctions`)を置き、ルート自体(`search-result-line`)を
      クリックするとその区間の種別・距離をポップアップ表示する
      (`docs/assets/graph.js`の`shortestPath`が返す`segments`に座標範囲
      `fromIdx`/`toIdx`を持たせ、`mergeShortSegments`で短い断片(20m未満)を
      隣の区間にまとめてから使う)。結果には所要時間の目安も表示する
      (`AVERAGE_CYCLING_SPEED_KMH = 15`km/hを仮定した単純計算、2026-09-22)
  - **分岐点マーカーは登録済みサイクリングロードだけ(2026-09-22)**: 以前は
    通常のtier1〜5ネットワークにも分岐点(IC/JCT風の白丸)を付けていたが、
    「高速道路風の方だけ残してほしい」との指摘を受け、
    `data/highway_routes.geojson`(登録済みサイクリングロード)のグラフから
    計算する`highway-route-junctions`/`highway-route-junction-label`に限定した
  - **地名検索(2026-09-21)**: 出発地・目的地を地図クリックだけでなく地名検索でも
    指定できるようにした。ジオコーダーはOpenStreetMapの無料サービス
    Nominatim(`nominatim.openstreetmap.org`、APIキー不要)を使用。
    入力するたびに自動検索する(ボタン不要)が、Nominatimの利用規約は
    「毎回のキー入力に反応するオートコンプリートは避けるべき」としているため、
    入力が止まってから600ms後に1回だけ検索するデバウンス処理で頻度を抑えている
    (`docs/assets/main.js`の`scheduleSearch`)
  - **経路の地図フィット(2026-09-21)**: 近距離検索・事前計算ルートどちらも、
    経路が求まったら自動でその範囲が見えるようにズーム・パンする
    (`fitRouteBounds`)。サイドバーが開いた状態で経路が隠れないよう、
    サイドバー幅分を左側paddingとして確保している
- **言語**: Python 3.x(データ処理)、JavaScript(フロント)
- **パッケージ管理**: `venv` + `pip`(`requirements.txt`)
- **実行環境**: Dockerでの再現可能な環境(R5用のJDKを含む)

## コーディング方針(src/以下)

- **コードを書き始める前に、必ず`src/<タスク名>/instruction.md`を新規作成または
  更新すること**。「目的」「計算・処理してほしいこと」「入出力」を先に明文化
  してからコードに着手する。データを渡された直後の探索的な確認であっても例外
  ではない(その場しのぎのスクリプトを`src/`外で書き始めて後から書き戻す、
  という順序は避ける)
- 可読性のため、役割ごとにモジュール・クラスを分ける
- 実験管理をしやすいようにする(対象地域・「専用」の定義・許容する一般道区間の
  長さなどのパラメータは設定ファイルに出し、コードの奥に埋め込まない。結果
  ファイル名に条件を反映する)
- **論理的な飛躍がないかを常に確認し、飛躍があればその理由と修正案を提示する
  こと**(上記「設計上の未決事項」はその実例)
- **分析・検証結果の出力先はすべて`results/`配下にすること**。リポジトリ直下に
  新しいトップレベルフォルダを勝手に作らない

## データの扱いに関する注意

- **100MB超のファイルはGitHubにpushできない**。OSMのPBFや中間ネットワークは
  容易にこれを超えるので、`data/raw/`・`data/interim/`はGit管理対象外
  (`.gitignore`)とし、取得スクリプトで再現できるようにする。50MB超のファイル
  をコミットしそうな場合は、コミット前にユーザーに確認すること
- **GitHub Pagesにはサイズ制限がある**(リポジトリ・サイト全体のサイズ目安が
  ある)。`docs/data/`に置く配信用データは軽量化(範囲の限定、座標の間引き、
  PMTiles/ベクトルタイル化など)を検討し、必要に応じてユーザーに相談する
- `data/datasets.txt`に、使用するすべてのデータの**出典URL・取得日・ライセンス・
  必要な出典表記**を記録する。フロントエンドの画面上にも、必要な出典表記
  (例: © OpenStreetMap contributors、各自治体データの出典)を必ず表示すること
- `data/raw/`が空の場合、生成AIが勝手に大量のデータをダウンロードしに行かず、
  まず取得対象・容量・ライセンスをユーザーに報告して確認すること
- `data/processed/`は「`data/raw/`から再計算できるもの」という前提で管理する

## Git・公開に関する注意

- **git configは生成AIが変更しないこと**(フックの有効化などが必要な場合は
  ユーザー自身が実行する)
- commit・pushは、ユーザーから明示的に指示された場合にのみ行う
  (無指示での自動commit・pushはしない。`log.md`の更新のみ例外として、
  ユーザーが許可した場合に自動pushしてよい【要ユーザー確認】)
- GitHub Pagesの公開設定・公開範囲(Publicリポジトリになる点を含む)は、
  ユーザーが判断する。公開前に、データのライセンス上、再配布が可能かを確認する
- APIキーやトークンをリポジトリにコミットしない(`.env`は`.gitignore`対象)

## log.mdの運用

- **絶対に必須**: コードやデータに変更を加えた作業は、その回のやり取りの中で
  (作業を終えたと報告する前に)必ず`log.md`に記録する。「今回は細かい修正
  だから」「時間が無いから」等の理由で省略しない。書き忘れに気づいた場合は
  次のターンに持ち越さず、その場で追記してから応答する(2026-09-22、
  ユーザーから「絶対に記載するように」との強い指示を受けて明文化)
- 生成AIとのやり取りを`log.md`に記録する。**エントリ見出しは「日付+時刻」単位**
  (例: `### 2026-09-20 15:40`)で、1回のやり取り・作業のまとまりごとに立てる
- 内容は**なるべく詳細に**(何を指示され、何を確認・判断し、どう対応し、結果
  どうなったかを省略しない)。「何を直したか」だけでなく「なぜそうなっていたか」
  も残す
- **新しい記載が一番上に来るように追記する**(下に追記しない)
- 意思決定(採用した公開方式、「専用」の定義、使用データの選定理由など)は
  必ずここに残す

## テスト・検証

- 対象地域の中の代表的な区間で、「自転車専用空間だけで実際につながっている
  経路」を手で確認できる検証用の出発地・目的地ペアを用意し、R5の出力と
  比較する(目視確認用の地図出力を`results/`に残す)
- ネットワークの連結性(連結成分の数・最大成分が全体に占める割合)を
  ビルドのたびに出力し、`results/tables/`に保存する
- 「専用」の判定ルール(タグ→種別の変換)は、入出力のペアをユニットテストとして
  固定する
